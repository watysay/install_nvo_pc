---

- name: a playbook to setup my local machine with my basic customizations
  hosts: localhost
  connection: local

  roles:
    - common
    - dotfiles
    - atom
    - git

#comment laptop if in a VM
...
